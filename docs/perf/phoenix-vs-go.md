# Phoenix vs Go — cross-language comparison

> **Placeholder.** This page is overwritten end-to-end by [`bench-corpus/run.sh`](../../bench-corpus/run.sh); the script is the single source of truth for both numbers *and* surrounding prose. Run it on a quiet machine with `hyperfine` + Go 1.22+ + `cargo` on `PATH` to populate the page:
>
> ```sh
> bash bench-corpus/run.sh
> git diff docs/perf/phoenix-vs-go.md
> ```
>
> Per [Phase 2.7 design decision E](../design-decisions.md#e-cross-language-comparison-scope-go-122-only) the page is informational only and never a regression gate — a stale placeholder is acceptable in the interim. Phoenix-vs-Phoenix numbers in [`docs/perf-baselines/`](../perf-baselines/) remain the gating signal. Refresh cadence: per-phase close (2.7, 2.4, 2.5 each refresh once).
