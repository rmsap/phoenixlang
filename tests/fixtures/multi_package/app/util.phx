// The app's OWN local `util` module — same name as `greet`'s internal `util`.
// Both coexist because the dependency's module is qualified as `greet.util`.
public function localValue() -> String { "1" }
